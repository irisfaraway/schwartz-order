<p id="notice"><%= notice %></p>

<h1>Listing Sides</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Spicy</th>
      <th>Price</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @sides.each do |side| %>
      <tr>
        <td><%= side.name %></td>
        <td><%= side.description %></td>
        <td><%= side.spicy %></td>
        <td>£<%= number_with_precision(side.price, :precision => 2) %></td>
        <td><%= link_to 'Show', side, class: 'button' %></td>
        <td><%= link_to 'Edit', edit_side_path(side), class: 'button' %></td>
        <td><%= link_to 'Destroy', side, method: :delete, data: { confirm: 'Are you sure?' }, class: 'button' %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Side', new_side_path, class: 'button' %>
