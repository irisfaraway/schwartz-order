<p id="notice"><%= notice %></p>

<h1>Listing Dips</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Spicy</th>
      <th>Price</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @dips.each do |dip| %>
      <tr>
        <td><%= dip.name %></td>
        <td><%= dip.spicy %></td>
        <td>£<%= number_with_precision(dip.price, :precision => 2) %></td>
        <td><%= link_to 'Show', dip, class: 'button' %></td>
        <td><%= link_to 'Edit', edit_dip_path(dip), class: 'button' %></td>
        <td><%= link_to 'Destroy', dip, method: :delete, data: { confirm: 'Are you sure?' }, class: 'button' %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Dip', new_dip_path, class: 'button' %>
